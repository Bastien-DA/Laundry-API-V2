model Person {
  id        String     @id @default(uuid()) @db.Uuid
  username  String     @db.VarChar(50)
  personType PersonType

  // Relations
  laundries Laundry[]
  machines  Machine[]

  user       User?

  @@map("persons")
}
